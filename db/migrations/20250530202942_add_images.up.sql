CREATE TABLE "images" (
    "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
    "user_id" INT REFERENCES users(id) ON DELETE SET NULL,
    "filename" VARCHAR(255) NOT NULL,
    "orig_ext" VARCHAR(5) NOT NULL,
    "created_at" TIMESTAMP DEFAULT now()
);
